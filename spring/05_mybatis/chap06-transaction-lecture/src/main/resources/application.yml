# datasource bean에 설정 될 DB 접속 정보
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/menudb
    username: swcamp
    password: swcamp

# mybatis config 설정
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.dbwls.transaction
  mapper-locations: mappers/**/*.xml

